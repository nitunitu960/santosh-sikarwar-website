# वेबसाइट अपडेट गाइड / Website Update Guide

यह गाइड बताती है कि फ़ोटो और दैनिक समाचार कैसे जोड़ें।
इसके लिए किसी कोडिंग की ज़रूरत नहीं है।

---

## 1) दैनिक समाचार / गतिविधि कैसे जोड़ें (Daily Feed)

फ़ाइल खोलें: **`data/feed.js`**

सबसे ऊपर एक नया ब्लॉक जोड़ें (नई पोस्ट सबसे ऊपर दिखेगी):

```js
{
  date: "16 अगस्त 2026",
  title: "यहाँ शीर्षक लिखें",
  text: "यहाँ पूरा विवरण लिखें।",
  image: "photo4.jpg"   // फ़ोटो का नाम, या खाली "" रखें
},
```

नियम:
- हर ब्लॉक के बाद कॉमा ( , ) ज़रूर लगाएँ।
- `image` में जो नाम लिखें, वह फ़ोटो `images/` फ़ोल्डर में होनी चाहिए।
- फ़ोटो न हो तो `image: ""` रखें।

---

## 2) फोटो गैलरी में फ़ोटो कैसे जोड़ें (Gallery)

चरण 1: फ़ोटो को **`images/`** फ़ोल्डर में डालें (जैसे `photo5.jpg`)।

चरण 2: फ़ाइल खोलें **`data/gallery.js`** और एक नया ब्लॉक जोड़ें:

```js
{ src: "photo5.jpg", caption: "कार्यक्रम का नाम" },
```

---

## 3) मुख्य (होम पेज) फ़ोटो कैसे लगाएँ

- फ़ोटो का नाम `photo.jpg` रखें और उसे `images/` फ़ोल्डर में डालें।
- (यह अपने आप होम पेज पर लगाने के लिए हमें एक बार सेट करना होगा — बताएँ तो कर देंगे।)

---

## 4) बदलाव को वेबसाइट पर लाइव कैसे करें

### तरीका A — मोबाइल/ब्राउज़र से (सबसे आसान, बिना सॉफ्टवेयर)
1. GitHub पर अपनी रिपॉज़िटरी खोलें।
2. फ़ोटो जोड़ने के लिए: `images` फ़ोल्डर खोलें → **Add file → Upload files** → फ़ोटो अपलोड करें → **Commit**।
3. समाचार/गैलरी अपडेट के लिए: `data/feed.js` या `data/gallery.js` खोलें →
   पेंसिल (✏️) आइकन दबाएँ → बदलाव करें → **Commit changes**।
4. 1–2 मिनट में वेबसाइट अपने आप अपडेट हो जाएगी।

### तरीका B — कंप्यूटर से (Git)
```powershell
git add .
git commit -m "नया अपडेट जोड़ा"
git push
```

---

## सुझाव
- फ़ोटो का साइज़ 1 MB से कम रखें ताकि साइट तेज़ चले।
- फ़ोटो के नाम में स्पेस न रखें (photo-1.jpg सही, "photo 1.jpg" गलत)।
