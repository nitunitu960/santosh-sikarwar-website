# वेबसाइट अपडेट गाइड / Website Update Guide

फ़ोटो और समाचार जोड़ने के दो तरीके हैं। कोई भी एक अपनाएँ।

---

## तरीका 1 — प्रबंधन पैनल से (सबसे आसान, फॉर्म भरकर)

एक बार सेटअप के बाद (देखें `CMS-SETUP.md`), बस यहाँ जाएँ:

**https://santoshsikarwar.in/admin/**

लॉगिन करें और फॉर्म भरकर समाचार, फ़ोटो, संपर्क जानकारी जोड़ें/बदलें।
फ़ोटो सीधे drag & drop से अपलोड होती है। **Publish** दबाते ही लाइव।

---

## तरीका 2 — फ़ाइल एडिट करके (बिना पैनल के)

### (क) दैनिक समाचार जोड़ें
फ़ाइल: **`data/feed.json`**
`"posts": [` के ठीक बाद एक नया ब्लॉक जोड़ें:

```json
{
  "date": "16 अगस्त 2026",
  "title": "यहाँ शीर्षक",
  "text": "यहाँ विवरण",
  "image": "images/photo4.jpg"
},
```

नियम: हर ब्लॉक के बाद कॉमा ( , ) लगाएँ। फ़ोटो न हो तो `"image": ""` रखें।

### (ख) गैलरी में फ़ोटो जोड़ें
1. फ़ोटो `images/` फ़ोल्डर में डालें (जैसे `photo5.jpg`)।
2. फ़ाइल **`data/gallery.json`** में `"photos": [` के बाद जोड़ें:

```json
{ "src": "images/photo5.jpg", "caption": "कार्यक्रम का नाम" },
```

### (ग) मुख्य फ़ोटो / संपर्क बदलें
फ़ाइल **`data/settings.json`** खोलें और `photo`, `email`, `phone`, `area` भरें।
उदाहरण: `"photo": "images/photo.jpg"`

---

## बदलाव लाइव कैसे करें (तरीका 2 के लिए)

### मोबाइल/ब्राउज़र से (बिना सॉफ्टवेयर)
1. GitHub पर रिपॉज़िटरी खोलें।
2. फ़ोटो: `images` फ़ोल्डर → **Add file → Upload files** → **Commit**।
3. समाचार/गैलरी: `data/feed.json` या `data/gallery.json` → पेंसिल (✏️) → edit → **Commit**।
4. 1–2 मिनट में वेबसाइट अपडेट हो जाएगी।

### कंप्यूटर से (Git)
```powershell
git add .
git commit -m "नया अपडेट"
git push
```

---

## लोकल प्रीव्यू के बारे में ज़रूरी बात
अब समाचार/गैलरी डेटा फ़ाइलों से लोड होते हैं, इसलिए सिर्फ़ फ़ाइल डबल-क्लिक
करने पर वे खाली दिख सकते हैं (ब्राउज़र सुरक्षा कारण)। सही प्रीव्यू के लिए:
- वेबसाइट को **लाइव (ऑनलाइन)** देखें, या
- एडिटर में "Live Server / Preview" चलाएँ।

बाकी डिज़ाइन डबल-क्लिक पर भी दिखता है।

---

## सुझाव
- फ़ोटो का साइज़ 1 MB से कम रखें।
- फ़ोटो के नाम में स्पेस न रखें (`photo-1.jpg` सही)।
